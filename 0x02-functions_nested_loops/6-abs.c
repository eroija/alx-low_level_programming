#include "main.h"
/**
  * int _abs(int) - computes the abs value of integer
  *
  * @n: the int in question
  *
  * Return: the absolute value the of integer
  */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
